# JACO CR v14

UI polish:
- Validation summary is only rendered when there are validation errors.
- The blank highlighted box on the New CR screen is removed.
- No database changes required.

The CR save flow now correctly generates the 10-digit CR ID using a direct SQL scalar command.

See `CHANGE-REGISTER.md` for the consolidated UI/UX change register.
